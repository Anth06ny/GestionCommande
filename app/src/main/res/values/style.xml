<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="android:colorActivatedHighlight">@color/selected_cellule_bg</item>
        <item name="colorControlHighlight">@color/selected_cellule_bg</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- ##################  -->
    <!-- GESTION BOUTON -->
    <!-- ################## -->
    <style name="bouton">
        <item name="android:textSize">@dimen/font_8</item>

    </style>

    <style name="button_reglage" parent="bouton">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="color_picker_bt">
        <item name="android:layout_margin">@dimen/margin_5</item>
        <item name="android:src">@drawable/circle_bg</item>
        <item name="android:layout_width">@dimen/standard_height</item>
        <item name="android:layout_height">@dimen/standard_height</item>
        <item name="android:background">?android:selectableItemBackground</item>
    </style>

    <!-- ##################  -->
    <!-- Dialog -->
    <!-- ################## -->
    <style name="dialog_text_libelle">
        <item name="android:textSize">@dimen/font_14</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">@dimen/standard_height</item>
        <item name="android:layout_height">@dimen/standard_height</item>
        <item name="android:background">?android:selectableItemBackground</item>
    </style>

    <!-- ##################  -->
    <!-- Accueil -->
    <!-- ################## -->

    <style name="acceuil_libelle">
        <item name="android:textSize">@dimen/font_14</item>
        <item name="android:textStyle">bold</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:textColor">@color/product_color</item>
    </style>

    <style name="acceuil_tv_note" parent="acceuil_libelle">
        <item name="android:textColor">@android:color/secondary_text_dark</item>
        <item name="android:textSize">@dimen/font_11</item>
    </style>

</resources>